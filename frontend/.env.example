# Public base URL of the future CPU-hosted FastAPI model service.
# Do not add secrets here: browser code cannot keep them private.
VITE_PREDICTION_API_URL=https://your-house-price-api.example.com

# Optional multimodal endpoint accepting multipart POST /analyze with `file` and `property`.
VITE_PLAN_ANALYSIS_API_URL=https://your-plan-analysis-api.example.com
